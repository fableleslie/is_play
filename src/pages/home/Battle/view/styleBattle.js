import styled from 'styled-components'

const BattleContainer = styled.div`
    height : 1.67rem;
    margin-top : .14rem;
    .space-carousel {
        padding: 16px;
        background: #DEF1E5;
        overflow: hidden;
    }
`

export default BattleContainer